# Credentials for the optional live integration tests (pytest -m integration).
# Copy to .env and fill in your MySOLEM account. The same values you use on
# mysolem.com. Region is "Europe" or "America".
email=
password=
region=Europe
